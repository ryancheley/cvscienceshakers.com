[project]
name = "cvscienceshakers-com"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "pelican[markdown]>=4.11.0",
]

[tool.setuptools.packages.find]
where = ["."]
exclude = ["themes*", "content*", "output*"]